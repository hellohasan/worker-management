<button type="button" class="btn {{ $btnClass }} btn-xs font-weight-bold text-uppercase {{ $classBtn }}" data-toggle="modal" data-target="#{{ $modal }}" data-id="{{ $id }}">
    <i class="{{ $icon }}"></i> {{ $text }}
</button>
